{% extends 'base.html.twig' %}

{% block title %}Albums index{% endblock %}

{% block body %}
    <h1>Albums index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Naam</th>
                <th>Artiest naam</th>
                <th>Nummers</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>

        {% for album in albums %}
            <tr>
                <td>{{ album.id }}</td>
                <td>{{ album.Naam }}</td>
                <td>{{ album.artiestenID.naam }} {{ album.artiestenID.achternaam  }}</td>
                <td></td>


{#                {% for nummer in nummers %}#}
{#                {% if nummer.albumID == album.id %}#}
{#                <td>{{ nummer.naam }}</td>#}
{#                {% endif %}#}
{#                {% endfor %}#}

                <td>
                    <a href="{{ path('albums_show', {'id': album.id}) }}">show</a>
                    {% if app.user %}
                        {% if app.user.isAdmin == "1" %}
                            <div class="mb-3">
                                <td><a href="{{ path('albums_edit', {'id': album.id}) }}">edit</a></td>

                        {% endif %}
                    {% endif %}

            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('albums_new') }}">Create new</a> <br/> <br />
    <a href="{{ path('home') }}">Back to homepage</a>
{% endblock %}
