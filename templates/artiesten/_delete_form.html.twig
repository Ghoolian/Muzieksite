<form method="post" action="{{ path('artiesten_delete', {'id': artiesten.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artiesten.id) }}">
    <button class="btn btn-lg btn-primary">Delete</button>
</form>
